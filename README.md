# sentinalapi
download more accuracy data

## How to Run

Double-click `run_sentinel.bat` to start the application. 
OR 
Run the following command in your terminal:
```bash
python api.py
```

### Linux / Mac / GitHub Codespaces
Run:
```bash
./run_sentinel.sh
```

**Do not run .gitignore**.

